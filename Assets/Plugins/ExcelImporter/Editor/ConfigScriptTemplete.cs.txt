// This code automatically generated by ExcelImporter

using System;
using UnityEngine;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace {{ cs_namespace }}
{
#if UNITY_EDITOR
    [Serializable]
#endif
    public partial class {{ config_name }}
    // : {{ baseclass_name }}
    {   {{ for field in fields }}{{ if !field.ignore }}
        {{ field.desc }}
        public {{ field.variate_type }} {{ field.variate_name }};
        {{end}}{{ end }}{{ for field in fields }}{{ if field.ignore }}
#if UNITY_EDITOR
        
        {{ field.desc }}
        public {{ field.variate_type }} {{ field.variate_name }};
        
#endif{{end}}{{ end }}
        public static List<{{config_name}}> Load(TextReader reader)
        {
            var result = new List<{{config_name}}>();
            //todo CSVParse 
            return result;
        }
        
    }
    
    public sealed partial class {{ config_manager }}
    //: CSVManagerBase
    {
        private readonly List<{{ config_name }}> _dataList;
        public {{ config_manager }}(TextReader reader)
        {
            _dataList = {{ config_name }}.Load(reader);
        }
    }
}